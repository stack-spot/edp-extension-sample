interface BuildUrlOptions {
  apiUrl: string,
  slug: string,
  version?: string,
  path?: string,
}

export function buildUrl({ apiUrl, slug, version, path }: BuildUrlOptions): string {
  return version ? `${apiUrl}/download/${slug}/${version}${path ?? ''}` : `${apiUrl}/download-latest/${slug}${path ?? ''}`
}
