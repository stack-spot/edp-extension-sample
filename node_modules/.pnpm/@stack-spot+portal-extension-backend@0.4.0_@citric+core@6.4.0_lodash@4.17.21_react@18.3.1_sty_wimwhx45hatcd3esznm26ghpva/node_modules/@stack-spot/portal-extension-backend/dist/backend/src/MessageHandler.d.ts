import { ExtensionConfigMessage } from '@stack-spot/portal-extension-commons';
interface ConstructorProps {
    slug: string;
    version?: string;
    apiUrl: string;
    iframe: HTMLIFrameElement;
    shouldAdaptHeight?: boolean;
}
export declare class MessageHandler {
    private iframe;
    private version;
    private apiUrl;
    private slug;
    private listener;
    private shouldAdaptHeight;
    private dataProvider;
    constructor({ apiUrl, iframe, slug, version, shouldAdaptHeight }: ConstructorProps);
    private postMessage;
    private getMessage;
    private setupListeners;
    private handleResizeMessage;
    private handleRequest;
    sendConfig(config: ExtensionConfigMessage): void;
    destroy(): void;
}
export {};
//# sourceMappingURL=MessageHandler.d.ts.map