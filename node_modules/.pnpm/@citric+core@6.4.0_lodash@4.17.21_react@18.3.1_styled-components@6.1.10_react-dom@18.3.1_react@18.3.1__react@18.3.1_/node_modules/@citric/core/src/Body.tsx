import { extendStyle, overwriteStyle, SxProp } from './component-style'
import { styled } from './styled'
import { defaultConfig } from './utils/config'
import { getColor, getFontStyle } from './utils/theme'

const Body = styled.body.withConfig(defaultConfig) <{ sx?: SxProp }>`
    background-color: ${({ theme }) => getColor(theme, 'light.400')};
    margin: 0;
    color: ${({ theme }) => getColor(theme, 'light.contrastText')};
    font: ${({ theme }) => getFontStyle(theme, 'body1')};

    @keyframes fadeIn {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    .no-scroll { overflow: hidden; }

    ${extendStyle('Body')};
    ${overwriteStyle('Body')};
`

export default Body
