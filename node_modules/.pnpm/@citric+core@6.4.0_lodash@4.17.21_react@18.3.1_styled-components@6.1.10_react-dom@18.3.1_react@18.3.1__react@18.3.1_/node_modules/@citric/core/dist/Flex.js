import Box from './Box.js';
import { extendStyle, overwriteStyle } from './component-style.js';
import { css, styled } from './styled.js';
import { defaultConfig } from './utils/config.js';
import { handleSxAliasProps } from './utils/sx-props.js';
import { getSpacing } from './utils/theme.js';
function getGapStyle(theme) {
    return css `
    margin: ${`-${getSpacing(theme, 3)}`};

    > ${Box} {
      padding: ${getSpacing(theme, 3)};
    }
  `;
}
const Flex = styled.div.withConfig(defaultConfig) `
  box-sizing: border-box;
  display: flex;

  ${extendStyle('Flex')};

  ${({ theme, gap }) => gap && getGapStyle(theme)}
  align-items: ${props => props?.alignItems};
  align-content: ${props => props?.alignContent};
  justify-items: ${props => props?.justifyItems};
  justify-content: ${props => props?.justifyContent};
  flex-wrap: ${props => props?.flexWrap || 'wrap'};
  flex-direction: ${props => props?.flexDirection};
  flex: ${props => props?.flex};

  ${handleSxAliasProps};
  ${overwriteStyle('Flex')};
`;
export default Flex;
//# sourceMappingURL=Flex.js.map