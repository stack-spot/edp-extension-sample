import { extendStyle, overwriteStyle } from './component-style.js';
import { styled } from './styled.js';
import { defaultConfig } from './utils/config.js';
import { getRadius } from './utils/theme.js';
const Image = styled.img.withConfig(defaultConfig) `
  box-sizing: border-box;
  max-width: 100%;
  vertical-align: middle;

  ${extendStyle('Image')};

  object-fit: ${(props) => props?.objectFit};
  border-radius: ${({ theme, radius }) => radius && getRadius(theme, radius)};

  ${overwriteStyle('Image')};
`;
export default Image;
//# sourceMappingURL=Image.js.map