import { extendStyle, overwriteStyle } from './component-style.js';
import { styled } from './styled.js';
import { defaultConfig } from './utils/config.js';
import { getColor, getRadius, getSpacing } from './utils/theme.js';
const Blockquote = styled.blockquote.withConfig(defaultConfig) `
  display: flex;
  padding-left: ${({ theme }) => getSpacing(theme, 5)};
  margin: ${({ theme }) => getSpacing(theme, 5)};

  &:before  {
    content: '';
    border: 2px solid ${({ theme, colorScheme, palette }) => getColor(theme, colorScheme && `${String(colorScheme)}.400` || palette || 'primary.400')};
    border-radius: ${({ theme }) => getRadius(theme, 'sm')};
    right: ${({ theme }) => getSpacing(theme, 5)};
    position: relative;
  }
  
  ${extendStyle('Blockquote')}
  ${overwriteStyle('Blockquote')}
`;
export default Blockquote;
//# sourceMappingURL=Blockquote.js.map