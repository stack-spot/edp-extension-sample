import { extendStyle, overwriteStyle } from './component-style.js';
import { styled } from './styled.js';
import { defaultConfig } from './utils/config.js';
import { getColor, getFontStyle } from './utils/theme.js';
const Body = styled.body.withConfig(defaultConfig) `
    background-color: ${({ theme }) => getColor(theme, 'light.400')};
    margin: 0;
    color: ${({ theme }) => getColor(theme, 'light.contrastText')};
    font: ${({ theme }) => getFontStyle(theme, 'body1')};

    @keyframes fadeIn {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    .no-scroll { overflow: hidden; }

    ${extendStyle('Body')};
    ${overwriteStyle('Body')};
`;
export default Body;
//# sourceMappingURL=Body.js.map