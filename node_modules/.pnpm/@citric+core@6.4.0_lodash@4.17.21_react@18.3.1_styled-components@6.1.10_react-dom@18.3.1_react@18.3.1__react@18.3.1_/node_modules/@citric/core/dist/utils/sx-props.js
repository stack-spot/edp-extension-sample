import { alias, sx } from '../sx.js';
export function handleSxAliasProps({ theme, ...props }) {
    const aliasProps = Object.entries(props).reduce((acc, [key, value]) => (alias[key] ? { ...acc, [key]: value } : acc), {});
    return sx(theme, aliasProps);
}
//# sourceMappingURL=sx-props.js.map