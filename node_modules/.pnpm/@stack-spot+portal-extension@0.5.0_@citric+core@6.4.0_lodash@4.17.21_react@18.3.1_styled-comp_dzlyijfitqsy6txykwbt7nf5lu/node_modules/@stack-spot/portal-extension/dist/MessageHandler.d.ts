import { ExtensionOperations, ExtensionRequestMessage, SuccessfulExtensionResponseMessage } from '@stack-spot/portal-extension-commons';
declare class MessageHandler {
    private promiseMap;
    private hasStarted;
    start(): void;
    private postMessage;
    private messageAsResponse;
    private setupListeners;
    sendRequest<T extends keyof ExtensionOperations>(request: ExtensionRequestMessage<T>): Promise<SuccessfulExtensionResponseMessage<T>>;
    resize(width: number, height: number): void;
}
export declare const messageHandler: MessageHandler;
export {};
//# sourceMappingURL=MessageHandler.d.ts.map