import { Text } from '@citric/core';
import { extendStyle, overwriteStyle } from '@citric/core/dist/component-style.js';
import { styled } from '@citric/core/dist/styled.js';
import { defaultConfig } from '@citric/core/dist/utils/config.js';
import { getFontStyle, getSpacing } from '@citric/core/dist/utils/theme.js';
export const FormHelper = styled(Text) `
  ${extendStyle('FormHelper')};
  margin-top: ${({ theme }) => getSpacing(theme, 2)};
  font: ${({ theme }) => getFontStyle(theme, 'body2')};
  ${overwriteStyle('FormHelper')};
`;
const FormItem = styled.div.withConfig(defaultConfig) `
  margin-bottom: ${({ theme }) => getSpacing(theme, 6)};
  ${extendStyle('FormItem')};
  ${overwriteStyle('FormItem')};
`;
export default FormItem;
//# sourceMappingURL=FormItem.js.map