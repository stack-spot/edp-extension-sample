import { extendStyle, overwriteStyle } from '@citric/core/dist/component-style.js';
import { styled } from '@citric/core/dist/styled.js';
import { defaultConfig } from '@citric/core/dist/utils/config.js';
import { getColor, getFontStyle, getSpacing } from '@citric/core/dist/utils/theme.js';
const FieldAddon = styled.div.withConfig(defaultConfig) `
  height: 2.5rem;
  display: flex;
  align-items: center;
  padding-inline: ${({ theme }) => `${getSpacing(theme, 3)}`};
  box-sizing: border-box;

  ${extendStyle('FieldAddon')};

  border: 1px solid ${({ theme }) => getColor(theme, 'light.600')};
  background: ${({ theme }) => getColor(theme, 'light.400')};
  font: ${({ theme }) => getFontStyle(theme, 'body2')};
  color: ${({ theme }) => getColor(theme, 'light.contrastText')};

  ${overwriteStyle('FieldAddon')};
`;
export default FieldAddon;
//# sourceMappingURL=FieldAddon.js.map