import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Flex, IconBox } from '@citric/core';
import { extendStyle, overwriteStyle } from '@citric/core/dist/component-style.js';
import forwardRefAs from '@citric/core/dist/forward-ref-as.js';
import { styled } from '@citric/core/dist/styled.js';
import { defaultConfig } from '@citric/core/dist/utils/config.js';
import { getColor, getFontStyle, getRadius, getSpacing } from '@citric/core/dist/utils/theme.js';
const StatusBadge = styled.div.withConfig(defaultConfig) `
  display: flex;
  height: ${({ theme }) => getSpacing(theme, 4)};
  width: ${({ theme }) => getSpacing(theme, 4)};
  background-color: ${({ theme, colorScheme = 'primary' }) => getColor(theme, colorScheme, 500)};
  border-radius: ${({ theme }) => getRadius(theme, 'full')};
  margin-left: ${({ theme }) => getSpacing(theme, 2)};
  margin-right: ${({ theme }) => getSpacing(theme, 2)};
`;
const StatusStyled = styled.div `
  display: inline-flex;
  align-items: center;
  font: ${({ theme }) => getFontStyle(theme, 'microtext1')};

  ${extendStyle('Status')};
  ${overwriteStyle('Status')};
`;
const StatusWithIcon = ({ colorScheme, icon: Icon }) => (Icon ? (_jsx(IconBox, { size: "xs", colorIcon: colorScheme, sx: { mx: '2' }, children: _jsx(Icon, {}) })) :
    _jsx(StatusBadge, { colorScheme: colorScheme }));
const Status = forwardRefAs(function Status({ statusPosition = 'right', colorScheme, icon, ...props }, ref) {
    return (_jsxs(StatusStyled, { ...props, ref: ref, children: [statusPosition === 'left' && _jsx(StatusWithIcon, { colorScheme: colorScheme, icon: icon }), _jsx(Flex, { as: "span", alignItems: "center", children: props.children }), statusPosition === 'right' && _jsx(StatusWithIcon, { colorScheme: colorScheme, icon: icon })] }));
});
export default Status;
//# sourceMappingURL=Status.js.map