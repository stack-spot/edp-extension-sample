import { extendStyle, overwriteStyle } from '@citric/core/dist/component-style.js';
import { defaultConfig } from '@citric/core/dist/utils/config.js';
import { handleSxAliasProps } from '@citric/core/dist/utils/sx-props.js';
import { getColor, getRadius, getSpacing } from '@citric/core/dist/utils/theme.js';
import { styled } from 'styled-components';
const Paper = styled.div.withConfig(defaultConfig) `
  padding: ${({ theme }) => `${getSpacing(theme, 7)}`};
  box-sizing: border-box;
  background-color: ${({ theme }) => getColor(theme, 'light.300')};
  border: 1px solid ${({ theme }) => getColor(theme, 'light')};
  border-radius: ${({ theme }) => getRadius(theme, 'sm')};

  ${handleSxAliasProps};
  ${extendStyle('Paper')};
  ${overwriteStyle('Paper')};
`;
export default Paper;
//# sourceMappingURL=Paper.js.map