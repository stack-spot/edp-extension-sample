import { HttpError } from '@oazapfts/runtime';
import { Event as EventType } from '../api/eventBus.js';
import { StackspotAPIError } from '../error/StackspotAPIError.js';
import { NetworkClient } from '../network/NetworkClient.js';
export declare const EVENTS_STORAGE_IDENTIFIER = "eventsError";
declare class EventBusClient extends NetworkClient {
    private queue;
    private deferred;
    private timeout;
    private lock;
    constructor();
    protected buildStackSpotError(error: HttpError): StackspotAPIError;
    private consume;
    private add;
    /**
     * Sends an event
     */
    sendEvent: (event: EventType) => Promise<void>;
}
export declare const eventBusClient: EventBusClient;
export {};
//# sourceMappingURL=event-bus.d.ts.map